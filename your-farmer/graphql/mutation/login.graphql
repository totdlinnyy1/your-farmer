mutation Login($email: String!, $password: String!) {
	login(options: { email: $email, password: $password }) {
		errors {
			message
		}
		user {
			id
			name
			lastname
			email
			role
			number
			avatarUrl
		}
	}
}
